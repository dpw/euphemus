Get struct parse to support all the pause states
1 after initial '{' (waiting for '}' or '"')
2 in member name
3a after member name (waiting for ':')
3b after colon (waiting for for non-whitespace)
4 after value (waiting for '}' or ',')

Add support for strings, numbers, arrays

Add support for variants

Add support for open structs

Check for repeated members

Error codes

Load a json schema

UTF-8 support